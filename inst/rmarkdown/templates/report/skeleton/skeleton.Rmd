---
title: "Untitled"
author:
  - name: '`r paste(params[["firstname"]], params[["lastname"]])`'
    affiliation: "Université de Lille, INSERM UMR 1283, CNRS UMR 8199, Institut Pasteur de Lille, EGID, F-59000, Lille, France."
    email: '`r paste0(params[["firstname"]], ".", params[["lastname"]], "@cnrs.fr")`'
date: '`r format(Sys.time(), "%B %d, %Y")`'
knit: (function(inputFile, encoding) rmarkdown::render(inputFile, output_file = gsub("[0-9]+-(.*)\\.Rmd", "\\1", basename(inputFile)), encoding = encoding, output_dir = here::here("reports"), intermediates_dir = here::here("outputs", gsub("[0-9]+-(.*)\\.Rmd", "\\1", basename(inputFile)))))
output: umr1283::html_report
params:
  firstname: "FirstName"
  lastname: "LastName"
  project_directory: !r here::here()
  output_directory: !r here::here("outputs", "999-default")
  data_directory: NULL
---

```{r setup, include = FALSE}
options(stringsAsFactors = FALSE, "width" = 110)


### Load packages ==================================================================================
library(here)

suppressPackageStartupMessages({
  library(knitr)
  library(sessioninfo)
  library(gt)
  library(ggplot2)
})


### Source functions ===============================================================================


### Environment ====================================================================================
project_name <- gsub("(.*)_.*", "\\1", basename(params[["project_directory"]]))
output_directory <- params[["output_directory"]]
dir.create(path = output_directory, recursive = TRUE, showWarnings = FALSE, mode = "0775")

opts_chunk$set(
  eval = TRUE,
  include = TRUE,
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  dpi = 120,
  tidy = FALSE,
  crop = TRUE,
  autodep = TRUE,
  results = "asis",
  fig.align = "center",
  width = getOption("width"),
  comment = "#>"
)


### Define theme ===================================================================================
options(
  ggplot2.discrete.colour = scale_colour_viridis_d,
  ggplot2.discrete.fill = scale_fill_viridis_d,
  ggplot2.continuous.colour = scale_colour_viridis_c,
  ggplot2.continuous.fill = scale_fill_viridis_c
)
theme_set(theme_light())
```

# Summary


# R session information

```{r session-info, results = "markup"}
session_info()
```
