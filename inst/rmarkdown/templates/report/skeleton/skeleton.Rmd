---
title: "Untitled"
author:
  - name: '`r paste(params[["firstname"]], params[["lastname"]])`'
    affiliation: | 
      "INSERM U1283, CNRS UMR 8199, European Genomic Institute for Diabetes (EGID), 
      Institut Pasteur de Lille, University of Lille, Lille University Hospital, Lille, F-59000, France."
    email: '`r paste0(params[["firstname"]], ".", params[["lastname"]], "@cnrs.fr")`'
date: '`r format(Sys.time(), "%B %d, %Y")`'
knit: (function(inputFile, encoding) rmarkdown::render(inputFile, output_file = gsub("[0-9-]*(.*)\\.Rmd", "\\1", basename(inputFile)), encoding = encoding, output_dir = here::here("reports"), intermediates_dir = here::here("outputs", gsub(".Rmd$", "", basename(inputFile)))))
output: umr1283::html_report
params:
  firstname: "FirstName"
  lastname: "LastName"
  project_directory: !r here::here()
  output_directory: !r here::here("outputs", "999-default")
  data_directory: NULL
---

```{r setup, include = FALSE}
### Environment ====================================================================================
options("stringsAsFactors" = FALSE, "width" = 110)

project_name <- gsub("(.*)_.*", "\\1", basename(params[["project_directory"]]))
output_directory <- params[["output_directory"]]
dir.create(path = output_directory, recursive = TRUE, showWarnings = FALSE, mode = "0775")


### Load packages ==================================================================================
suppressPackageStartupMessages({
  library(knitr)
  library(sessioninfo)
  
  library(gt)
  library(ggplot2)
})


### knitr settings =================================================================================
opts_chunk$set(
  eval = TRUE,
  include = TRUE,
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  dpi = 120,
  tidy = FALSE,
  crop = TRUE,
  autodep = TRUE,
  results = "asis",
  fig.align = "center",
  width = getOption("width"),
  comment = "#>",
  fig.path = file.path(output_directory, "images/")
)


### Define theme ===================================================================================
options(
  ggplot2.discrete.colour = function(...) scale_colour_viridis_d(..., begin = 0.15, end = 0.85),
  ggplot2.discrete.fill = function(...) scale_fill_viridis_d(..., begin = 0.15, end = 0.85),
  ggplot2.continuous.colour = function(...) scale_colour_viridis_c(..., begin = 0.15, end = 0.85),
  ggplot2.continuous.fill = function(...) scale_fill_viridis_c(..., begin = 0.15, end = 0.85)
)
theme_set(
  theme_light() +
    theme(
      plot.title.position = "plot",
      plot.caption.position = "plot",
      plot.title = element_text(),
      plot.subtitle = element_text(face = "italic", size = rel(0.8)),
      plot.caption = element_text(face = "italic", size = rel(0.5))
    )
)


### Source functions ===============================================================================


```

# Summary


# R session information

```{r session-info, results = "markup"}
session_info()
```
