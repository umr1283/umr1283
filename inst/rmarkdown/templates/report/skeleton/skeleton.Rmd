---
title: "Untitled"
params:
  firstname: "FirstName"
  lastname: "LastName"
  script_name: "999-default"
  project_directory: !r here::here()
  data_directory: NULL
author:
  - name: '`r paste(params[["firstname"]], params[["lastname"]])`'
    affiliation: "Université de Lille, INSERM UMR 1283, CNRS UMR 8199, Institut Pasteur de Lille, EGID, F-59000, Lille, France."
    email: '`r paste0(params[["firstname"]], ".", params[["lastname"]], "@cnrs.fr")`'
date: '`r format(Sys.time(), "%B %d, %Y")`'
knit: 
  (function(inputFile, encoding) {
    rmarkdown::render(
      input = inputFile,
      output_file = gsub("[0-9]+-(.*)\\.Rmd", "\\1", inputFile),
      encoding = encoding, 
      output_dir = here::here("reports"),
      intermediates_dir = here::here("outputs")
    )
  })
output: umr1283::html_report
---

```{r setup, include = FALSE}
options(stringsAsFactors = FALSE, "width" = 110)


### Load packages ==================================================================================
# library(here)

suppressPackageStartupMessages({
  library(sessioninfo)
  library(gt)
  library(knitr)
  library(ggplot2)
})


### Source functions ===============================================================================


### Environment ====================================================================================
project_name <- gsub("(.*)_.*", "\\1", basename(params[["project_directory"]]))

# if (dir.exists(file.path(params[["working_directory"]], "outputs"))) {
#   output_directory <- file.path(params[["working_directory"]], "outputs", params[["script_name"]])
# } else {
#   output_directory <- file.path(params[["working_directory"]], "Data", params[["script_name"]])
# }
# dir.create(path = output_directory, recursive = TRUE, showWarnings = FALSE, mode = "0775")

opts_chunk$set(
  results = "asis",
  include = TRUE,
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  dpi = 120,
  tidy = FALSE,
  crop = TRUE,
  autodep = TRUE,
  fig.align = "center",
  width = getOption("width"),
  comment = "#>"
)


### Define theme ===================================================================================
theme_set(theme_light())
```

# Summary


# R session information

```{r session-info, results = "markup"}
session_info()
```
