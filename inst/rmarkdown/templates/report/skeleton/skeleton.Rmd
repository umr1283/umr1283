---
title: "Untitled"
params:
  firstname: "FirstName"
  lastname: "LastName"
  project_directory: !r here::here()
  data_directory: NULL
author:
  - name: '`r paste(params[["firstname"]], params[["lastname"]])`'
    affiliation: | 
      "INSERM U1283, CNRS UMR 8199, European Genomic Institute for Diabetes (EGID), 
      Institut Pasteur de Lille, University of Lille, Lille University Hospital, Lille, F-59000, France."
    email: '`r paste0(params[["firstname"]], ".", params[["lastname"]], "@cnrs.fr")`'
date: '`r format(Sys.time(), "%B %d, %Y")`'
knit: (function(inputFile, encoding) {file <- rmarkdown::render(inputFile, encoding = encoding); file.rename(from = file, to = here::here("reports", basename(file)))})
output: umr1283::html_report
---

```{r setup, include = FALSE}
options("width" = 110)


### Project Setup ==================================================================================
library(here)
project_name <- gsub("(.*)_.*", "\\1", list.files(params[["project_directory"]], pattern = ".Rproj$"))


### Load Packages ==================================================================================
suppressPackageStartupMessages({
  library(knitr)
  library(sessioninfo)
  
  library(ragg)
  # library(ggplot2)
  # library(ggtext)
  # library(patchwork)
  # library(data.table)
  
  # library(gt)
})


### knitr settings =================================================================================
opts_chunk$set(
  include = TRUE, # default: TRUE
  eval = TRUE, # default: FALSE
  echo = FALSE, # default: TRUE
  warning = FALSE, # default: TRUE
  message = FALSE, # default: TRUE
  autodep = TRUE, # default: FALSE
  dpi = 120, # default: 72
  comment = "#>",
  dev = "ragg_png",
  width = getOption("width")
)


### Tables and Figures Theme =======================================================================
# options(
#   ggplot2.discrete.colour = function(...) scale_colour_viridis_d(..., begin = 0.15, end = 0.85),
#   ggplot2.discrete.fill = function(...) scale_fill_viridis_d(..., begin = 0.15, end = 0.85),
#   ggplot2.continuous.colour = function(...) scale_colour_viridis_c(..., begin = 0.15, end = 0.85),
#   ggplot2.continuous.fill = function(...) scale_fill_viridis_c(..., begin = 0.15, end = 0.85)
# )
# theme_set(theme_minimal())
# theme_update(
#   plot.title.position = "plot",
#   plot.caption.position = "plot",
#   plot.title = element_markdown(),
#   plot.subtitle = element_markdown(face = "italic", size = rel(0.80)),
#   plot.caption = element_markdown(face = "italic", size = rel(0.65)),
#   axis.title.x = element_markdown(),
#   axis.text.x = element_markdown(),
#   axis.title.y = element_markdown(),
#   axis.text.y = element_markdown()
# )


### Functions ======================================================================================


```

# Summary


# R Session Information

```{r session-info, results = "markup"}
session_info()
```
